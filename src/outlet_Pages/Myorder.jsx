import React from 'react';

const Myorder = () => {
    return (
        <div>
            this is the logged in user order page here they can see their order history
        </div>
    );
};

export default Myorder;